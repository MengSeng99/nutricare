// home.dart
import 'package:flutter/material.dart';

class SpecialistSchedulesScreen extends StatelessWidget {
  const SpecialistSchedulesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Specialist Schedule Screen', style: TextStyle(fontSize: 24)));
  }
}